'use strict';

class Form extends Component {
    constructor() {
        super();
    }

    init() {
        return this.element;
    }
    //new Modal('Congrats', new DeliveryConfirmed(data).init());
}