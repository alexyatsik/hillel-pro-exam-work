'use strict';

window.addEventListener('DOMContentLoaded', windowHandler);

$('#modal-window-btn').addEventListener('click', modalWindowOpenHandler);

$('#close-btn').addEventListener('click', modalWindowCloseHandler);